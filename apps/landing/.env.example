# MySQL (same DB as legacy PHP CRM)
DATABASE_URL="mysql://USER:PASSWORD@HOST:3306/DATABASE"

# Admin login: use this password with any email (e.g. admin@senior-floors.com)
ADMIN_PASSWORD="your-secure-password"

# Session cookie signing (optional; set in production)
# SESSION_SECRET="random-long-secret"

# Optional: SMTP for lead notification emails (future)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=contact@senior-floors.com
# SMTP_PASS=your_app_password
